{
  "name": "web-dev-spring-2023",
  "version": "1.0.0",
  "description": "A web based application for the Spring 2023 Web Development class at SUNY New Paltz",
  "license": "BSD",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jewpaltz/web-dev-spring-2023.git"
  },
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js --watch server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i -ws && npm run build -ws"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
